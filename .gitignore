# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env
/api/.env
api/firebase-service-account.json
/backend/.env
backend/firebase-service-account.json

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Signature des apps (ne jamais committer)
keystore.properties
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision

# Anciens projets natifs Capacitor : restent sur le disque (keystore.properties inclus) mais ne sont plus suivis
web-app/android/
web-app/ios/

# Expo (app mobile) : les dossiers natifs sont régénérés par `expo prebuild`
app/.expo/
app/dist/
app/web-build/
app/ios/
app/android/
app/.env*.local
