<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Trainwise Admin' %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<div class="layout">
  <aside class="sidebar">
    <div class="sidebar-brand">
      <div class="brand-title">Train<span>wise</span></div>
      <div class="brand-sub">Back-office</div>
    </div>
    <nav class="sidebar-nav">
      <span class="nav-label">Navigation</span>
      <a href="/dashboard" class="nav-item <%= page === 'dashboard' ? 'active' : '' %>">
        <span class="nav-icon">📊</span> Dashboard
      </a>
      <a href="/users" class="nav-item <%= page === 'users' ? 'active' : '' %>">
        <span class="nav-icon">👥</span> Utilisateurs
      </a>
      <a href="/users?role=coach" class="nav-item <%= page === 'coaches' ? 'active' : '' %>">
        <span class="nav-icon">🏃</span> Coaches
      </a>
    </nav>
    <div class="sidebar-footer">
      <a href="/logout" class="btn-logout">
        <span>🚪</span> Déconnexion
      </a>
    </div>
  </aside>
  <main class="main">
    <div class="topbar">
      <div class="topbar-title"><%= title || 'Trainwise Admin' %></div>
      <div class="topbar-meta">Trainwise &mdash; Panneau d'administration</div>
    </div>
    <div class="content">
